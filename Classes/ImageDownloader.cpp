#include "ImageDownloader.h"

USING_NS_CC;


// on "init" you need to initialize your instance
bool ImageDownloader::initWithURL(std::string url)
{
    //////////////////////////////
    // 1. super init first
    if ( !Sprite::init() )
    {
        return false;
    }
    
    
    
    return true;
}
