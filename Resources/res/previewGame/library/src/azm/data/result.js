/**
 * Holds all data for the game results.
 * The instance is created and then the properties are set.
 */

AZM.Data.Result = function()
{
    this.zoomCount = 0;
    this.distance = 0;
    this.newRecord = false;
};
